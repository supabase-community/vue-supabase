# Playground

This folder contains a minimal Vue 3 app for testing the local package.

## Run locally

```bash
cd playground
npm install
npm run dev
```

The app uses the local package from the repository root via `file:..`.
