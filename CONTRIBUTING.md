# Contributing

Thanks for helping improve vue-supabase.

## Development

1. Install dependencies with `npm install`.
2. Run the suite with `npm test`.
3. Run type checking with `npm run typecheck`.
4. Run formatting checks with `npm run format:check`.

## Pull requests

Please keep changes focused and include tests when introducing behavior changes.
